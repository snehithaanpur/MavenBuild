# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml


trigger:
- develop

steps:
- checkout: self
  persistCredentials: true
- script: git config --global user.name "snehithaanpur"
- script: git config --global user.email "snehithaanpur456@gmail.com"
- script: $ git remote set-url origin https://github.com/kiotie32/artbit-text.git
- script: git push new origin --force

          

